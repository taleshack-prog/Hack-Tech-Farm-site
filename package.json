{
  "name": "hacktechfarm-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site institucional e hub de produtos da Hack Tech Farm",
  "scripts": {
    "build": "python3 tools/build.py",
    "dev": "python3 -m http.server 4000",
    "check": "node --check js/site.js && node --check js/data.js && node --check js/catalog.js && node --check js/auth.js && node --check js/dashboard.js && node --check js/login.js && node --check api/_lib.js && node --check api/subscribe.js && node --check api/contact.js"
  },
  "engines": { "node": ">=20" },
  "license": "UNLICENSED"
}
